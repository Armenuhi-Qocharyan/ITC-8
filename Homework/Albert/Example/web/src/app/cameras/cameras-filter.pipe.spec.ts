import { CamerasFilterPipe } from './cameras-filter.pipe';

describe('CamerasFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CamerasFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
