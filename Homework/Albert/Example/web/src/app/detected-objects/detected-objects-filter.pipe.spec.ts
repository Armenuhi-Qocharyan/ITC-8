import { DetectedObjectsFilterPipe } from './detected-objects-filter.pipe';

describe('DetectedObjectsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DetectedObjectsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
